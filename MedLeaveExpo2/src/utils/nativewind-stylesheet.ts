import { StyleSheet } from 'react-native';
import { create } from 'nativewind/stylesheet';

// Este archivo se puede utilizar para forzar la creación del stylesheet de NativeWind
// y asegurarse de que los estilos se apliquen correctamente

export const nativeWindStyleSheet = create();